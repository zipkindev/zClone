This local Zclone distribution does not configure a release service, so
`selfupdate` is unavailable. Build a replacement locally with `make zclone`
and install it using your normal local deployment process.
